// Array
let list: Array<number> = [1, 2, 3];

// Assertions
let csomeValue: any = "this is a string";
let cstrLength: number = (<string>someValue).length;
let cstrLength2: number = (someValue as string).length;
